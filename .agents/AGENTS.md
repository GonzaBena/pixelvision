# Reglas del Proyecto

- Utilizar **siempre** `pnpm` en lugar de `npm` o `yarn` para instalar dependencias, ejecutar scripts y correr pruebas (ej. `pnpm install`, `pnpm test`, `pnpm run build`, `pnpm run typecheck`).
